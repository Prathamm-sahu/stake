import BetPanel from '@/components/BetPanel'
import { FC } from 'react'

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return (
    <div>
      <BetPanel />
    </div>
  )
}

export default page